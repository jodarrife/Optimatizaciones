import { FiltroDocentePipe } from './filtro-docente.pipe';

describe('FiltroDocentePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDocentePipe();
    expect(pipe).toBeTruthy();
  });
});
