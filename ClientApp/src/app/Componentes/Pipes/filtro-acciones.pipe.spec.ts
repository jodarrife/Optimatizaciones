import { FiltroAccionesPipe } from './filtro-acciones.pipe';

describe('FiltroAccionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAccionesPipe();
    expect(pipe).toBeTruthy();
  });
});
