import { FiltroTipoActividadPipe } from './filtro-tipo-actividad.pipe';

describe('FiltroTipoActividadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTipoActividadPipe();
    expect(pipe).toBeTruthy();
  });
});
